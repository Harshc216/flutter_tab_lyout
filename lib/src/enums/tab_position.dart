/// Defines where the tab bar should be displayed.
enum TabPosition {
  /// Displays the tab bar at the top.
  top,

  /// Displays the tab bar at the bottom.
  bottom,

  /// Displays the tab bar on the left side.
  left,

  /// Displays the tab bar on the right side.
  right,
}